<?php

return [
    /*
     * Зворотне злиття: підтягувати в систему правки, які зробили просто в
     * Google Таблиці, перед тим як перезаписати аркуш.
     *
     * Вимкнено навмисно (рішення користувача 2026-09-22): поки що таблиця має
     * лишатись дзеркалом сервісу, інакше двосторонній обмін тільки плутає.
     * Увімкнути — PLANS_SHEET_PULL=true в .env, код робочий і під тестами.
     *
     * Службову колонку з ID і зліпок вивантаження експорт пише й з вимкненим
     * злиттям — саме вони роблять вмикання миттєвим: без них перший прогін
     * після вмикання вважав би всі рядки аркуша новими.
     */
    'sheet_pull' => env('PLANS_SHEET_PULL', false),
];
